
<%=render :layout => 'common/mobile',
          :locals => { :page_id => mobile_page_id() , :page_title => "Listado de Unidades de Medida" } do %> 

  <div role="main" class="ui-content">   

	 <%= link_to 'Nueva Unidad de Medida', new_measure_unit_path %> &nbsp;&nbsp; <%= link_to 'Volver', "/welcome/configuration" %>
	<table class="tabla_listado" cellpadding=0 cellspacing=0>
	  <tr class="tabla_listado_fila_cabezal">
	    <th>Nombre</th>
	    <th>Símbolo</th> 
		<th></th>
	  </tr>
	 
	  <% @measure_units.each do |measure_unit| %>
		<%  item_url = measure_unit_path(measure_unit)
		    item_id=mobile_item_id(item_url) %>
	      
		<tr id="<%= item_id %>" class="tabla_listado_fila_contenido">

	      <td><%= measure_unit.name %></td>
	      <td><%= measure_unit.symbol %></td>
	      <td><%= link_to image_tag(asset_path ('buttons/view.gif')), measure_unit %> &nbsp; <%= link_to image_tag(asset_path ('buttons/edit.gif')), edit_measure_unit_path(measure_unit) %> &nbsp; <%= link_to image_tag(asset_path('buttons/delete.gif')),"javascript:list_delete_resource_dialog('" + item_url + "', '" + item_id + "')"    %></td>
	 	
	    </tr>
	  <% end %>
	</table>
	<div class="links_listado"><%= link_to 'Volver', "/welcome/configuration" %></div>

  </div> <!-- /content --> 
  
<% end %>

