
<%=render :layout => 'common/mobile',
          :locals => { :page_id => mobile_page_id() , :page_title => "Listado de Tipos de Retorno" } do %> 

  <div role="main" class="ui-content">  

   <%= link_to 'Nuevo Tipo de Retorno', new_return_type_path %> &nbsp;&nbsp; <%= link_to 'Volver', "/welcome/configuration" %>
  <table class="tabla_listado" cellpadding=0 cellspacing=0>
    <tr class="tabla_listado_fila_cabezal">
      <th>Descripción</th>
	  <th></th>
    </tr>
   
    <% @return_types.each do |return_type| %>


        <%  item_url = return_type_path(return_type)
            item_id=mobile_item_id(item_url) %>
       
		    <tr id="<%= item_id %>" class="tabla_listado_fila_contenido"> 
          <td><%= return_type.description %></td>
	       <td><%= link_to image_tag(asset_path ('buttons/view.gif')), return_type %> &nbsp; <%= link_to image_tag(asset_path ('buttons/edit.gif')), edit_return_type_path(return_type) %> &nbsp; <%= link_to image_tag(asset_path('buttons/delete.gif')), "javascript:list_delete_resource_dialog('" + item_url + "', '" + item_id + "')" %></td>
   	
       </tr>
    <% end %>
    
  </table>

  <div class="links_listado"><%= link_to 'Volver', "/welcome/configuration" %></div>  

  </div><!-- /content --> 
  
<% end %>



